/** Parent shell uses the same centered floating-rail geometry as the director shell */
export {
  directorContent as parentContent,
  directorMain as parentMain,
  directorRailWidth as parentRailWidth,
  directorShellGrid as parentShellGrid,
  directorShellOuter as parentShellOuter,
} from "@/lib/business/layout";
